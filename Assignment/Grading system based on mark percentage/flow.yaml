Start
  |
  ↓
Read mark
  |
  ↓
if (mark < 0)
  |
  ↓
Display "Invalid"
  |
  ↓
else if (mark >= 90)
  |
  ↓
Display "O"
  |
  ↓
else if (mark >= 80 && mark < 90)
  |
  ↓
Display "A"
  |
  ↓
else if (mark >= 70 && mark < 80)
  |
  ↓
Display "B"
  |
  ↓
else
  |
  ↓
Display "Slow Learners"
  |
  ↓
End
