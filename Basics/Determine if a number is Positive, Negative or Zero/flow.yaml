Start
  |
  ↓
Read number
  |
  ↓
if (number == 0)
  |
  ↓
Display "Number is zero."
  |
  ↓
else if (number > 0)
  |
  ↓
Display "Number is positive."
  |
  ↓
else
  |
  ↓
Display "Number is negative."
  |
  ↓
End
